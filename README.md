# SideScape Releases

Encrypted distribution artifacts for SideScape development builds.

This repository intentionally contains no SideScape source code and no plaintext plugin JARs. The launcher downloads the encrypted artifact referenced by `latest.properties`; authorized testers decrypt it locally with their separately provided `access.txt` key.
